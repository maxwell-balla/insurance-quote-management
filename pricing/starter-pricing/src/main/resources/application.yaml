server:
  port: 8081

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/iqmrealm

  mongodb:
    host: localhost
    port: 27017
    database: price_db
    username: price_user
    password: price_pw
    authentication-database: price_db

logging:
  level:
    root: INFO
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
    com.playground: DEBUG
    org.springframework.security: DEBUG